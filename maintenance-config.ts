/**
 * Configuration for maintenance mode.
 * Set enabled to true to redirect all users to the maintenance page.
 */
export const MaintenanceConfig = {
  enabled: false, // Cambiar a false para habilitar el sistema nuevamente
};
